package com.skillstorm.restclient;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.*;


//@SpringBootTest(classes = MockFeignClient.MockProductClient.class, webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
public class ProductClientTest {
//
//    @Autowired
//    private ProductClient productClient;
//
//    @Test
//    void findById() {
//        assertEquals(productClient.findById(1).getName(), "Thin");
//
//    }
}